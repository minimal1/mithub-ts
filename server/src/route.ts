/** @format */
type Routes = { api: string; accessToken: string };

const API: string = "/api";
const ACCESS_TOKEN: string = "/access_token";

const routes: Routes = {
  api: API,
  accessToken: ACCESS_TOKEN,
};

export default routes;
